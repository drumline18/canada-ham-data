[build]
builder = "nixpacks"

[deploy]
startCommand = "python server.py"
healthcheckPath = "/status"
# Allow slow cold starts; /status returns 200 as soon as Flask is listening.
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
